// typecasting

var x = "44";
console.log(parseInt(x), typeof parseInt(x));


// ternary operater
// condition ? if-true : if-false
// you can also nest ternary operators
var a = 0.5;
var rand = Math.random();
a > rand ? console.log("a is bigger than rand") : console.log("rand is bigger than a");
